<br>
<h3>Only the big'uns!!! Catches over 15 pounds.</h3><br>
<% @fish.each do |f|%>
    <% if f.type && f.angler%>
<div>
    <p><%= f.angler.username%> caught a <%= f.type.name%>, with ID of, <%= f.id%> that weighs <%= f.weight%>lbs.</p>
</div>
    <%end%>
<%end%><br>

