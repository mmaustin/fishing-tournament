<br>

<% flash.each do |type, msg| %>
  <div>
    <%= msg %>
  </div>
<% end %><br>

<p><%= current_user.username%>, you may edit the fish's type below.</p><br>

<% if @type%>
  <%= render 'form' %>
<% else%>
    <h3>This type doesn't exist.</h3>
<%end%>